apply plugin: "war"

bootWar {
    mainClass = "com.demo.opensociety.CommunicationServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
